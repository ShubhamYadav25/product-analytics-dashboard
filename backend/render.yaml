services:
  - type: web
    name: analytics-dashboard-api
    runtime: node
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: analytics-db
          property: postgresql://analytics_user:qGrgZx5EXFai6c1I04SpA5OXbDQS0uCZ@dpg-d6e5qqktgctc73fdcus0-a/analytics_sqe0
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://your-frontend-url.vercel.app # Will update after frontend deploy
      - key: PORT
        value: 10000